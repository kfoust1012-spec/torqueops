import { formatDesignLabel } from "../design/tokens";

export function formatDispatchCalendarScopeLabel(value: string | null | undefined) {
  return formatDesignLabel(value);
}

export function formatDispatchConflictTypeLabel(value: string | null | undefined) {
  return formatDesignLabel(value);
}

export function formatDispatchCalendarViewLabel(value: string | null | undefined) {
  return formatDesignLabel(value);
}
