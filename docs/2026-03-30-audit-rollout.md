# 2026-03-30 Audit Rollout

This checklist tracks the audit-driven product compression pass so the work stays tied to the findings instead of drifting into isolated visual tweaks.

## Current Status

- `Dispatch`: complete
- `Visits`: complete
- `Customers`: complete
- `Fleet`: complete
- `Supply`: complete
- `Finance`: complete
- `Cross-desk thread / IA cleanup`: complete

## Audit Checklist

### 1. Compress top-of-page chrome

- [x] Dispatch top stack compressed and focus mode introduced
- [x] Visits top stack compressed, idle bulk UI hidden, queue chrome reduced
- [x] Customers active-thread workspace starts higher with less switcher dominance
- [x] Finance top summary stack compressed into queue/file-first layout
- [x] Fleet top chrome reduced; map mode now uses a single on-map command strip, a tighter map-only lens strip, hidden shell-level desk labels, collapsible map controls, and a priority-lane roster that only appears when map pressure is real
- [x] Supply top chrome reduced; stock/setup pressure now behaves more like a compact utility, the unblock thread bar stays shorter outside setup mode, setup utility only appears when sourcing readiness is the active risk, and setup remains tucked behind compact disclosure instead of leading the desk

### 2. Make Dispatch unquestionably central

- [x] Same-day fit and recovery made more visible in Dispatch
- [x] Release runway surfaced directly inside Dispatch
- [x] ETA / timing update actions moved into Dispatch board context
- [x] Same-day insertion and approved-release moves now carry explicit crew-readiness packets before commitment
- [x] Closeout recovery now has a guided Dispatch runbook with direct file-open, reminder, and drawer actions
- [x] Recovery surface now uses progressive disclosure, a compact utility rail, a sharper single next-move strip, a collapsed overflow for lower-priority watchlists, a trimmed top intervention summary, and a true one-priority recovery order where approved release, same-day fit, closeout recovery, lane-release posture, and lane-recovery posture tuck behind utility disclosures whenever hotter work already exists, including in focus mode and while an active thread is open

### 3. Push Visits toward queue-plus-thread

- [x] More estimate work moved into the active visit thread
- [x] Visits drawer/header chrome compressed
- [x] Inactive bulk UI hidden until selection exists
- [x] Queue slices, row framing, and drawer furniture are progressively disclosed; non-selected rows hide more default secondary actions, notes, estimate facts, and utility controls, the visit thread keeps more sections compact by default, support-tab drawer files keep only the primary artifact visible before a compact overflow, and customer-quote / handoff / follow-through rails only appear when the work is actually due
- [x] Thread-open Visits now tucks slice/scope/filter and view controls behind compact triggers while reducing drawer support furniture and support-file sprawl further through compact summaries and tighter default-open behavior

### 4. Make Customers thread-first

- [x] Active customer thread starts higher
- [x] Service-site and commercial-account context promoted
- [x] Switch/search layer now collapses hard when a thread is already active, with the active sidebar header removed, visible meta phrasing trimmed, the switcher demoted into the left rail as a compact utility, non-active registry rows carrying less contact/address weight, and the active-thread sidebar now leaving overflow records behind the switcher while the active case file owns the main canvas

### 5. Make Fleet truly map-first and readiness-first

- [x] Team / unit language converged into Fleet
- [x] Collapse pre-map layers so the map becomes the first real operating surface
- [x] Strengthen crew readiness packet and exception-first hierarchy
- [x] Remove remaining pre-map command posture so map, crew-readiness packet, and the on-map command strip dominate even harder; map controls collapse by default, the priority-lane roster no longer forces itself open in map mode, and calm single-lane map states hide the roster entirely

### 6. Demote Estimates into visit-thread support

- [x] Estimate runway moved into the visit thread
- [x] Visit thread now owns more builder / send / approval / release work
- [x] Standalone Estimates now behaves like a compact visit-approval support queue with collapsible filters, thinner header chrome, trimmed toolbar metrics, lighter inspector chrome, and less peer-desk gravity

### 7. Rebuild Supply around unblock-now pressure

- [x] Supply header reframed around unblock-now
- [x] Supply first screen now leads with live service-thread pressure
- [x] Split unblock-now lanes from stock-control / routing-foundation utility behavior
- [x] Setup/configuration posture stays demoted unless sourcing readiness is actually degrading live unblock flow, and stock control remains a utility unless it is the active risk

### 8. Tighten Finance into a dense closeout desk

- [x] Finance top summary stack compressed
- [x] Queue/file starts higher
- [x] Compact filter panel added so queue/file starts sooner
- [x] Finance queue rows and closeout file chrome are denser and more visit-thread-led; the selected file now leads with the visit thread, keeps the customer thread adjacent to that action strip, and pushes invoice workspace entry behind the live service thread

### 9. Cross-desk continuity and removal

- [x] Carried case file / hot-thread layer promoted
- [x] Promise / trust / site / closeout continuity now travels as a carried case-file ledger
- [x] Dispatch and Visits saved slices added with progressive disclosure
- [x] Customer / fleet / unit / vehicle language cleaned up further
- [x] Visible `Jobs` residue and peer-desk estimate gravity reduced further across dashboards, dispatch errors, Fleet assignment notices, parts follow-through, and approval support copy
- [x] Visible `workspace` / meta-desk phrasing removed further from shell, support, and customer-thread copy, with shell-level desk framing trimmed and supply shell wording simplified

## Completion Note

This audit checklist is complete. Any additional changes from here should be treated as new product work or a fresh audit pass rather than leftover items from the 2026-03-30 findings.
