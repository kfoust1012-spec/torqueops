# Audit 2026-03-30 Rollout

## Goal
- Finish the shift from organized multi-desk software into a field-service operating system with less ceremony, tighter hierarchy, and one carried service thread from intake through closeout.

## Status
- [x] Shared continuity primitives added for active service thread, promise confidence, route confidence, service-site thread, and release runway.
- [x] Carried case file expanded to expose the shared active thread contract in the shell and hot-thread API.
- [x] Today Brief compressed toward an owner exception brief.
- [x] Dispatch tightened around one dominant board with subordinate recovery and release-runway utilities.
- [x] Visits tightened toward queue plus thread with shared continuity state in the active thread.
- [x] Customers pushed further toward relationship thread plus site thread.
- [x] Fleet pushed further toward map-first route control with explicit route confidence.
- [x] Supply rebuilt more aggressively around unblock-now pressure.
- [x] Finance rebuilt more aggressively around dense closeout queue plus file rhythm.
- [x] Standalone Estimates demoted further into visit-thread support only.
- [x] Cross-desk wording cleanup reduced user-facing `Jobs` and meta-workspace residue to compatibility-only internals.

## This Tranche
- [x] Extended `apps/web/lib/service-thread/continuity.ts` so the shared active thread now carries a primary desk, dominant next move, drawer/file targets, and richer service-site thread facts instead of stopping at basic continuity labels.
- [x] Reworked `apps/web/app/api/internal/hot-thread/route.ts` and `apps/web/components/ui/admin-shell.tsx` so the carried case file now exposes primary-desk ownership, direct file/context jumps, and richer site-thread continuity in both the shell UI and contextual command layer.
- [x] Tightened `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx`, and `apps/web/app/dashboard/visits/page.tsx` again so Today Brief starts with a tighter owner brief, Dispatch hides more secondary chrome in support-planning/focus conditions, and Visits collapses redundant thread affordances.
- [x] Tightened `apps/web/app/dashboard/supply/page.tsx`, `apps/web/app/dashboard/finance/page.tsx`, and `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so setup pressure yields to live unblock work, Finance trims thread-bar duplication, and Customers promotes vehicle/site continuity higher in the active thread utility layer.
- [x] Added `apps/web/lib/service-thread/continuity.ts` and tests for shared thread continuity models.
- [x] Extended `apps/web/app/api/internal/hot-thread/route.ts` to return a canonical `activeThread`.
- [x] Expanded `apps/web/components/ui/admin-shell.tsx` and command search to treat site thread and focus-mode actions as first-class.
- [x] Reduced `apps/web/app/dashboard/page.tsx` toward owner-exception behavior.
- [x] Wired shared release runway, promise confidence, and site-thread summaries into `apps/web/app/dashboard/visits/page.tsx`.
- [x] Wired shared site-thread continuity into `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx`.
- [x] Wired shared route confidence into Dispatch same-day insertion in `apps/web/app/dashboard/dispatch/page.tsx` and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx`.
- [x] Wired shared route confidence into Fleet readiness in `apps/web/app/dashboard/fleet/_components/fleet-types.ts`, `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx`, and `apps/web/app/dashboard/fleet/_components/fleet-live-map.tsx`.
- [x] Compressed `apps/web/app/dashboard/page.tsx` further by removing the owner-brief command strip and reducing Dispatch watch weight.
- [x] Reworked `apps/web/app/dashboard/supply/page.tsx` so stock control and routing foundation fall back to utility treatment unless they are the active unblock risk.
- [x] Reworked `apps/web/app/dashboard/finance/page.tsx` into a denser closeout queue/file rhythm with fewer top metrics and less row-chip duplication.
- [x] Tightened `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` so saved slices and surface-mode chrome drop away when queue or thread work is already owning the surface.
- [x] Tightened `apps/web/app/dashboard/visits/page.tsx` so queue slices collapse harder behind the active visit thread instead of staying exposed above the queue.
- [x] Demoted `apps/web/app/dashboard/estimates/page.tsx` further into estimate-support behavior with visit-return-first actions and lighter toolbar weight.
- [x] Added a formal `Release runway` Dispatch surface in `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` so approved estimate placement is a first-class operating mode instead of only a recovery subsection.
- [x] Aligned release-runway wording across `apps/web/app/dashboard/dispatch/_components/dispatch-command-deck.tsx`, `apps/web/app/dashboard/estimates/page.tsx`, `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/supply/page.tsx`, `apps/web/components/ui/admin-shell.tsx`, and `apps/web/lib/estimates/support.ts`.
- [x] Scrubbed more lingering visible `Jobs` copy in `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/parts/suppliers/page.tsx`, `apps/web/app/dashboard/visits/[jobId]/estimate/workspace/page.tsx`, and `apps/web/lib/procurement/providers/service.ts`.
- [x] Strengthened `apps/web/app/dashboard/supply/page.tsx` around explicit source-path gaps, substitute-path pressure, ETA risk, and unblock-first copy inside the main lane content and utility rail.
- [x] Tightened `apps/web/app/dashboard/finance/page.tsx` around service-closeout continuity by carrying quote-thread context into the queue rows, file strip, file actions, and empty file state.
- [x] Demoted `apps/web/app/dashboard/estimates/page.tsx` further into visit-approval support with lower toolbar gravity, visit-first wording, and conditional layout switching.
- [x] Compressed `apps/web/app/dashboard/page.tsx` further into a narrower owner brief by cutting header actions, shrinking the visible dispatch watch, reducing queue preview depth, and limiting the side rail to the hottest intervention section.
- [x] Tightened `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` so saved slices, queue visibility, and the operations rail subordinate more aggressively to the primary board surface, while the focus bar now carries explicit promise-confidence and route-confidence signals.
- [x] Compressed `apps/web/app/dashboard/visits/page.tsx` harder toward queue-plus-thread by making the hero compact whenever live queue work exists and by removing thread-open header actions that were not immediately hot.
- [x] Promoted route confidence higher in `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` by surfacing it directly in the map command bar instead of leaving it mostly inside the inspector and readiness packet.
- [x] Expanded `apps/web/app/api/internal/hot-thread/route.ts` so customer and invoice carried-case payloads now carry shared route-confidence snapshots instead of leaving route continuity empty outside visit threads.
- [x] Expanded `apps/web/components/ui/admin-shell.tsx` so the carried case file renders route confidence alongside promise confidence, release runway, and site thread continuity.
- [x] Reworked `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` so the customer thread consumes shared promise-confidence and route-confidence snapshots directly instead of rendering desk-local promise labels only.
- [x] Reworked `apps/web/app/dashboard/dispatch/_components/dispatch-operations-rail.tsx` so the selected intervention focus now carries shared promise-confidence and route-confidence signals plus direct customer-thread and site-thread continuity jumps.
- [x] Reworked `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx`, `apps/web/app/dashboard/fleet/_components/fleet-live-map.tsx`, and `apps/web/app/dashboard/fleet/_components/fleet-types.ts` so the map command bar and crew readiness packet now use shared promise-confidence and route-confidence continuity, with a direct visit-thread handoff from the selected stop.
- [x] Reworked `apps/web/app/api/internal/jobs/[jobId]/snapshot/route.ts` and `apps/web/app/dashboard/dispatch/_components/dispatch-quick-edit-panel.tsx` so the Dispatch quick-edit drawer now consumes shared promise-confidence, route-confidence, release-runway, and site-thread continuity instead of a drawer-local status vocabulary, with direct visit/customer/site thread jumps.
- [x] Reworked `apps/web/lib/fleet/workspace.ts`, `apps/web/app/dashboard/fleet/_components/fleet-types.ts`, and `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` so Fleet team and unit support cards now surface lane-level route confidence and promise confidence instead of falling back to route-health-only summaries.
- [x] Reworked `apps/web/app/dashboard/finance/page.tsx` so the closeout file now carries shared promise confidence, release runway, route continuity, and site-thread context with direct visit/customer/site thread jumps instead of only finance-local labels.
- [x] Reworked `apps/web/app/dashboard/visits/page.tsx` so the active visit thread header now exposes shared promise confidence, route confidence, and release-runway posture directly in the split-pane summary instead of burying continuity lower in the drawer.
- [x] Reworked the Visits support artifact surfaces in `apps/web/app/dashboard/visits/page.tsx` so field, supply, and invoice support files now carry shared thread continuity and direct customer/site thread jumps instead of behaving like detached local artifacts.
- [x] Compressed the active customer hero in `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` so the relationship thread bar owns more of the action weight and the hero carries fewer parallel top-level actions.
- [x] Reworked the visit invoice detail and edit files in `apps/web/app/dashboard/visits/[jobId]/invoice/page.tsx` and `apps/web/app/dashboard/visits/[jobId]/invoice/edit/page.tsx` so the deep-work billing surfaces now keep direct customer and site thread continuity instead of reading like isolated invoice workspaces.
- [x] Reworked the estimate builder entry and shell in `apps/web/app/dashboard/visits/[jobId]/estimate/workspace/page.tsx` and `apps/web/app/dashboard/visits/[jobId]/estimate/workspace/_components/estimate-workspace-shell.tsx` so the deep-work quote builder now reads as an estimate thread on the live service thread, with direct customer and site thread jumps.
- [x] Compressed `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx`, and `apps/web/app/dashboard/visits/page.tsx` again so Today Brief hides quiet support panels, Dispatch tucks board slices behind utility treatment sooner, and Visits collapses visible slice chrome into one queue-controls disclosure.
- [x] Scrubbed another visible wording tranche in `apps/web/app/dashboard/layout.tsx` and `apps/web/app/dashboard/page.tsx` by replacing remaining user-facing `job` and `command center` phrasing with visit- and desk-led language.
- [x] Scrubbed more visible desk/settings wording in `apps/web/app/dashboard/dispatch/settings/page.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-calendar-settings-form.tsx`, and `apps/web/app/dashboard/settings/page.tsx` so remaining `Jobs` and `command center` copy keeps collapsing into visit- and dispatch-desk language.
- [x] Compressed `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so the active customer hero loses its duplicated top action row and the relationship thread bar owns more of the action load.
- [x] Compressed `apps/web/app/dashboard/supply/page.tsx` again so stock control and routing-foundation support fall back to compact utility disclosures whenever they are not the active unblock risk.
- [x] Compressed `apps/web/app/dashboard/finance/page.tsx` again so the closeout thread bar loses its fallback visit action and secondary customer/site/quote jumps collapse behind a compact continuity disclosure.
- [x] Compressed `apps/web/app/dashboard/page.tsx` again so Today Brief carries one smaller header summary and one primary header action before the real exception surface starts.
- [x] Compressed `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so focus-bar secondary actions fall away when intervention layers are already open and selected-thread utility actions collapse behind a utility disclosure.
- [x] Compressed `apps/web/app/dashboard/visits/page.tsx` again so the Visits hero drops the always-visible Dispatch jump when a thread is open and queue controls show less slice chrome while the thread is already dominant.
- [x] Compressed `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` once more so the right follow-through rail drops away while recovery or release runway are already owning the surface, and remaining Dispatch summary labels use less self-aware wording.
- [x] Compressed `apps/web/app/dashboard/visits/page.tsx` once more so the hero drops its status badge whenever a visit thread is already open.
- [x] Compressed `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx`, and `apps/web/app/dashboard/visits/page.tsx` again so Today Brief loses a redundant dispatch link, the intervention rail reads less like a labeled sub-page, release-runway links disappear when that surface is already active, and the Visits hero drops more explanation once a thread is open.
- [x] Compressed `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/visits/page.tsx`, and `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so Today Brief drops the last stale action-rail phrasing, the Visits thread bar moves its third action behind utility treatment, and the active customer thread bar collapses secondary actions behind a compact `More` disclosure.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/edit/page.tsx` and `apps/web/app/dashboard/visits/[jobId]/estimate/page.tsx` so the remaining visit deep-work files now carry shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps instead of reading like local edit or estimate exceptions.
- [x] Compressed `apps/web/app/dashboard/supply/page.tsx` again so the supply thread bar keeps one hot action visible and pushes the secondary jump behind a compact `More` disclosure.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/inspection/page.tsx`, `apps/web/app/dashboard/visits/[jobId]/photos/page.tsx`, and `apps/web/app/dashboard/visits/[jobId]/inventory/page.tsx` so the remaining visit utility files now carry shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps instead of reading like local inspection, evidence, or stock pages.
- [x] Compressed `apps/web/app/dashboard/visits/[jobId]/inventory/page.tsx` again so stock lookup falls behind a compact utility disclosure and the active service thread owns more of the first-screen context.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/parts/page.tsx` so visit sourcing now carries shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps instead of reading like a standalone procurement page.
- [x] Compressed `apps/web/app/dashboard/visits/[jobId]/parts/page.tsx` and `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so visit-inventory jumps and remaining customer inspector rail labels fall behind quieter utility treatment.
- [x] Reworked `apps/web/app/dashboard/parts/requests/[requestId]/page.tsx` so the live supply request file now carries shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps instead of reading like a local request admin page.
- [x] Compressed `apps/web/app/dashboard/parts/requests/[requestId]/page.tsx`, `apps/web/app/dashboard/finance/page.tsx`, and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so provider/settings jumps, Finance thread-bar secondary actions, and Dispatch focus-bar secondary actions fall behind utility treatment when the primary canvas is already owning the screen.
- [x] Reworked `apps/web/app/dashboard/parts/carts/[cartId]/page.tsx` so the supplier cart now carries shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps whenever it is attached to a single active visit.
- [x] Compressed `apps/web/app/dashboard/supply/page.tsx` and `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so secondary stock-control, routing-foundation, and selected-customer inspector actions read more like quiet utilities and less like parallel panels.
- [x] Reworked `apps/web/app/dashboard/parts/purchase-orders/[purchaseOrderId]/page.tsx` so the purchase-order file now carries shared promise confidence, route or release posture, site-thread continuity, and direct visit/customer/site thread jumps whenever it is attached to a single active visit.
- [x] Compressed `apps/web/app/dashboard/parts/purchase-orders/[purchaseOrderId]/page.tsx` again so the external order URL falls behind utility treatment instead of competing with the primary fulfillment actions.
- [x] Compressed `apps/web/app/dashboard/supply/page.tsx` again so the last raised priority reminder and extra stock/setup actions flatten into the same utility grammar as the rest of the Supply rail.
- [x] Compressed `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so the workspace sidebar no longer repeats a duplicate customer-list header before the actual customer-thread list card.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/invoice/edit/page.tsx` again so the billing file now uses thread/file language instead of editor/workspace language, with quieter billing-state copy and less separate-product framing.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/estimate/workspace/page.tsx` and `apps/web/app/dashboard/visits/[jobId]/estimate/workspace/_components/estimate-workspace-shell.tsx` again so the estimate deep-work surfaces now read as an estimate thread/file on the service thread, with less builder/workspace wording and quieter parts-thread utility copy.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/estimate/page.tsx`, `apps/web/app/dashboard/inventory/locations/[locationId]/page.tsx`, and `apps/web/app/dashboard/inventory/transfers/[transferId]/page.tsx` again so the remaining visible estimate/inventory deep-work labels drop `workspace` wording in favor of thread, record, and ledger language.
- [x] Reworked `apps/web/app/dashboard/parts/supplies/page.tsx`, `apps/web/app/dashboard/parts/suppliers/page.tsx`, `apps/web/app/dashboard/parts/integrations/page.tsx`, and `apps/web/app/dashboard/parts/integrations/partstech/page.tsx` again so remaining user-facing Supply wording drops procurement/workspace phrasing in favor of supply-desk and supply-flow language.
- [x] Compressed `apps/web/app/dashboard/visits/[jobId]/estimate/page.tsx` again so the estimate detail intro no longer keeps a non-hot `Source parts` action exposed in the top row when stronger service-thread actions already exist.
- [x] Reworked `apps/web/app/dashboard/_components/service-history-panel.tsx` and `apps/web/app/dashboard/_components/service-history-view.tsx` again so service-history surfaces use quieter service-thread wording and less filter chrome.
- [x] Compressed `apps/web/app/dashboard/page.tsx` and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so Today Brief summarizes one clearer next move and Dispatch trims focus signals harder whenever recovery, queue, or the right rail is already open.
- [x] Compressed `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/visits/page.tsx`, and `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx` again so Today Brief trims its header brief to the hottest two stats, Visits pushes full queue filters behind utility treatment while a thread is already open, and Customers limits the active-thread signal row to the two strongest cues.
- [x] Compressed `apps/web/app/dashboard/layout.tsx`, `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/visits/page.tsx`, `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx`, and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so the shell drops the remaining `Office home` caption, Today Brief loses another explanatory support line, the open Visits thread keeps one dominant action with the rest behind utility treatment, and Customers/Dispatch use quieter utility labels instead of support-panel wording.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/page.tsx` and `apps/web/app/dashboard/parts/supplies/[listId]/page.tsx` again so the visit deep-record route drops the lingering self-explaining workspace callout and `Back to workspace` wording, while the supply-list detail reads more like a reusable supply file on the desk and less like a local admin page.
- [x] Scrubbed another visible cross-desk wording tranche in `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx`, `apps/web/app/dashboard/inventory/page.tsx`, `apps/web/app/dashboard/parts/requests/[requestId]/page.tsx`, and `apps/web/app/dashboard/visits/[jobId]/page.tsx` so remaining `job` phrasing keeps collapsing into visit-led language.
- [x] Compressed `apps/web/app/dashboard/page.tsx`, `apps/web/app/dashboard/visits/page.tsx`, and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so Today Brief drops the last extra header/explainer band, Visits shortens queue-state copy while a thread is already open, and Dispatch uses quieter visit-queue/release-runway labels inside recovery actions.
- [x] Reassessed the core-desk compression tracker and closed `Today Brief`, `Dispatch`, and `Visits`, then trimmed `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx`, `apps/web/app/dashboard/supply/page.tsx`, and `apps/web/app/dashboard/finance/page.tsx` again so Fleet header copy is quieter, Supply drops a redundant top-of-screen action, and Finance removes a duplicate queue-caption line.
- [x] Compressed `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` and `apps/web/app/dashboard/finance/page.tsx` again so Fleet drops the extra non-map lens-summary line and Finance trims the queue header down to one compact filters disclosure, while `apps/web/app/dashboard/parts/integrations/page.tsx` and `apps/web/app/dashboard/parts/suppliers/page.tsx` lose the last visible `Back to workspace` wording in favor of `Back to supply desk`.
- [x] Compressed `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` again so the non-map lens card stops explaining itself, and scrubbed the remaining visible `Back to workspace` wording in `apps/web/app/dashboard/parts/carts/[cartId]/page.tsx` and `apps/web/app/dashboard/parts/purchase-orders/[purchaseOrderId]/page.tsx` in favor of `Back to supply desk`.
- [x] Scrubbed another visible cross-desk wording tranche in `apps/web/app/dashboard/dispatch/_components/dispatch-calendar-grid.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-calendar-settings-form.tsx`, `apps/web/app/dashboard/fleet/_components/fleet-live-map.tsx`, `apps/web/app/dashboard/parts/integrations/page.tsx`, `apps/web/app/dashboard/parts/integrations/amazon-business/page.tsx`, and `apps/web/app/dashboard/parts/integrations/repairlink/page.tsx` so backlog, quick-edit, Fleet map, and Supply integration copy keep collapsing into visit-led and supply-flow language.
- [x] Reworked `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` again so secondary command-deck cards only render when route pressure is actually hot, which keeps non-map Fleet surfaces reading more like route-control utilities and less like persistent support panels.
- [x] Reworked `apps/web/app/dashboard/supply/page.tsx` again so the priority reminder and inventory brief yield to live unblock pressure instead of staying visible as setup-first first-screen furniture.
- [x] Reworked `apps/web/app/dashboard/finance/page.tsx` again so the closeout file drops the duplicated `next move` detail block and uses denser thread-first empty-file copy.
- [x] Reworked `apps/web/app/dashboard/visits/[jobId]/invoice/page.tsx` and `apps/web/app/dashboard/visits/[jobId]/invoice/edit/page.tsx` again so both invoice drawers now carry direct visit/customer/site thread jumps instead of behaving like isolated billing drawers.
- [x] Tightened `apps/web/app/dashboard/dispatch/_components/dispatch-quick-edit-panel.tsx` so appointment confirmations key off the active promise window, not only the scheduled-start timestamp, and updated `apps/e2e/tests/web-write-flows.spec.ts` to cover the current scheduled-dispatch drawer path honestly.
- [x] Re-verified the full web regression suite after the Dispatch drawer fix, with `pnpm test:e2e:web:ci` back to green at 13/13 passing.
- [x] Extended `apps/web/app/dashboard/fleet/_components/fleet-types.ts`, `apps/web/lib/fleet/workspace.ts`, and `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` so the Fleet inspector now keeps service-site continuity and direct visit/customer/site thread jumps on the selected route stop instead of trapping that context inside Dispatch or Customers.
- [x] Extended `apps/web/app/dashboard/supply/page.tsx` so the hot unblock thread now carries direct visit/customer/site thread jumps from the Supply thread bar utility, using live job thread context instead of only desk-to-desk hops.
- [x] Tightened `apps/web/app/dashboard/finance/page.tsx` again so queue rows read more explicitly as next-closeout threads, the selected file elevates release-runway handoff when a quote is approved, and the empty state speaks in stalled-thread language instead of generic waiting-room copy.
- [x] Tightened `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` again so focus and intervention states collapse secondary thread badges, alerts, and lane/parts utilities sooner, and surface-mode switching yields into utility treatment instead of competing with the board.
- [x] Reworked `apps/web/app/dashboard/visits/_components/visits-workboard.tsx` and `apps/web/app/dashboard/visits/page.tsx` again so the queue uses one thread-first action instead of a redundant thread button, while focus mode hides drawer tab switching and utility-strip chrome unless those secondary surfaces are actively in use.
- [x] Tightened `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx` again so map controls and priority-lane roster yield sooner to the active inspector, while the lane inspector collapses thread jumps and technician contact into one utility layer instead of a standing support strip.
- [x] Tightened `apps/web/app/dashboard/supply/page.tsx` again so routing-foundation utility yields completely whenever live unblock pressure is already active, instead of competing with the unblock-now desk posture.
- [x] Reworked `apps/web/app/dashboard/finance/page.tsx` again so the closeout file keeps only the strongest continuity signals exposed by default and tucks quote/owner/next-move detail behind utility treatment, which makes the file read more like a closeout thread than a support panel.
- [x] Re-verified the full web regression suite after the Fleet/Supply/Finance intervention-first pass, including rerunning `pnpm test:e2e:web:ci` cleanly after a transient `.next` lock bootstrap issue.
- [x] Reworked `apps/web/app/dashboard/supply/_components/supply-workspace-shell.tsx` and `apps/web/app/design-system.css` again so nested Supply routes no longer carry a broad workspace-style header; they now keep one unblock-first action visible and push Dispatch/Inventory/desk-path navigation behind utility treatment.
- [x] Cleaned `apps/web/app/dashboard/customer-vehicles/page.tsx` so the compatibility-only redirect no longer carries the old explicit customer-workspace mode flag, which further reduces hidden gravity for the standalone `Customer Vehicles` concept.
- [x] Reworked `apps/web/app/dashboard/supply/_components/supply-workspace-shell.tsx`, `apps/web/app/dashboard/supply/page.tsx`, and `apps/web/app/design-system.css` again so Supply now opens with a smaller unblock-first shell, hides setup utility unless sourcing setup is already the active lens, shortens the live unblock thread summary, and compresses provider/foundation cards so the desk reads less like layered setup navigation.
- [x] Reworked `apps/web/app/dashboard/finance/page.tsx` and `apps/web/app/design-system.css` again so Finance now promotes the closeout file when only one or two active threads are visible, keeps one dominant file action with release/collection/thread jumps behind utility treatment, and compresses the queue/file layout into a denser closeout-first surface.
- [x] Re-verified the full web regression suite after the Supply and Finance compaction pass, including rerunning `pnpm test:e2e:web:ci` cleanly after stopping local dev servers that were holding the CI ports.
- [x] Reworked `apps/web/app/dashboard/visits/_components/visits-workboard.tsx`, `apps/web/app/dashboard/visits/page.tsx`, `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx`, and `apps/web/app/design-system.css` again so Visits now hides advanced filters until requested, drops non-primary row overflow in focus mode, trims the selected-thread signal stack, Dispatch focus mode collapses secondary focus-bar actions/signals harder, and shared empty/severe states render denser and more severe instead of soft or roomy.
- [x] Re-verified the full web regression suite after the interaction/severe-state pass, including clearing a stale `apps/web/.next/lock` before rerunning `pnpm test:e2e:web:ci` cleanly.
- [x] Finished the final subtraction sweep in `apps/web/app/dashboard/supply/page.tsx`, `apps/web/app/dashboard/customers/_components/customers-workspace-shell.tsx`, `apps/web/app/dashboard/fleet/_components/fleet-workspace.tsx`, and `apps/web/app/dashboard/dispatch/_components/dispatch-command-center.tsx` so duplicate Supply CTAs are gone, customer/site actions collapse behind utility treatment, the Fleet no-units empty state keeps one dominant move, and same-day Dispatch cards hide lower-priority lane detail behind a compact reveal instead of stacking it at full height.
- [x] Re-verified `pnpm --filter @mobile-mechanic/web typecheck`, `pnpm --filter @mobile-mechanic/web build`, and `pnpm test:e2e:web:ci` after the final subtraction sweep with 13/13 Playwright tests passing.

## Remaining Highest-Value Work
- No remaining user-facing audit gaps are open in this rollout tracker.
- Remaining residue is limited to compatibility routes, internal naming, and non-user-facing implementation structure.

## Verification
- `pnpm --filter @mobile-mechanic/web test -- lib/service-thread/continuity.test.ts app/api/internal/command-search/route.test.ts`
- `pnpm --filter @mobile-mechanic/web typecheck`
- `pnpm --filter @mobile-mechanic/web build`
- `pnpm test:e2e:web:ci`
